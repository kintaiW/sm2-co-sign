# SM2 协同签名服务 - 前端界面菜单设计文档

## 1. 概述

### 1.1 文档目的
本文档定义 SM2 协同签名服务的管理后台前端界面菜单结构、页面布局及交互设计规范。

### 1.2 适用范围
- 管理后台前端开发
- UI/UX 设计参考
- 前端代码架构设计

---

## 2. 菜单结构

### 2.1 一级菜单

| 菜单名称 | 图标 | 权限要求 | 说明 |
|---------|------|---------|------|
| 仪表盘 | DashboardOutlined | 所有角色 | 系统概览与关键指标 |
| 密钥管理 | KeyOutlined | 安全管理员 | 密钥生成、导入、管理 |
| 签名服务 | EditOutlined | 运维管理员 | 签名请求与管理 |
| 用户管理 | UserOutlined | 超级管理员 | 用户账户管理 |
| 系统设置 | SettingOutlined | 超级管理员 | 系统配置管理 |
| 操作日志 | FileTextOutlined | 运维管理员/超级管理员 | 审计与日志查询 |

### 2.2 二级菜单结构

```
📁 仪表盘
   └── 系统概览

📁 密钥管理
   ├── 密钥列表
   ├── 密钥生成
   └── 密钥导入

📁 签名服务
   ├── 签名请求
   └── 签名记录

📁 用户管理
   ├── 用户列表
   ├── 角色管理
   └── 权限配置

📁 系统设置
   ├── 基础配置
   ├── 安全策略
   └── 服务监控

📁 操作日志
   ├── 操作记录
   └── 登录日志
```

---

## 3. 页面布局设计

### 3.1 仪表盘 - 系统概览

**路径**: `/dashboard`

**布局**: 
```
┌─────────────────────────────────────────────────────────────┐
│  [头部统计卡片区域]                                           │
│  ┌───────┐  ┌───────┐  ┌───────┐  ┌───────┐               │
│  │密钥总数│  │今日签名│  │活跃用户│  │服务状态│               │
│  └───────┘  └───────┘  └───────┘  └───────┘               │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────────────────┐  ┌──────────────────────┐        │
│  │    签名趋势图表       │  │    密钥分布图表       │        │
│  │    (最近7天/30天)     │  │    (按状态/类型)      │        │
│  └──────────────────────┘  └──────────────────────┘        │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐   │
│  │                 最近操作记录                         │   │
│  │  时间 | 用户 | 操作类型 | 详情 | 状态               │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

**功能要点**:
- 实时统计数据展示
- 签名数量趋势图 (ECharts/Chart.js)
- 密钥状态分布饼图
- 最近10条操作记录快速预览

---

### 3.2 密钥管理

#### 3.2.1 密钥列表

**路径**: `/keys/list`

**布局**:
```
┌─────────────────────────────────────────────────────────────┐
│  [搜索栏]                                                    │
│  关键词: [________] 状态: [下拉选择] 类型: [下拉选择]        │
│  [搜索] [重置]                                [生成密钥]     │
├─────────────────────────────────────────────────────────────┤
│  [数据表格]                                                  │
│  ┌─────┬──────────┬──────┬────────┬────────┬──────┬──────┐ │
│  │选择 │ 密钥ID    │ 类型  │ 创建时间│ 状态   │ 操作者│ 操作 │ │
│  ├─────┼──────────┼──────┼────────┼────────┼──────┼──────┤ │
│  │ □  │ key_001  │ SM2  │ 2024-01│ 启用   │ admin │ 详情 │ │
│  │ □  │ key_002  │ SM2  │ 2024-02│ 禁用   │ user1 │ 详情 │ │
│  └─────┴──────────┴──────┴────────┴────────┴──────┴──────┘ │
├─────────────────────────────────────────────────────────────┤
│  [分页]  共100条  每页[10▼]条  < 1 2 3 ... 10 >            │
└─────────────────────────────────────────────────────────────┘
```

**API映射**: `GET /mapi/keys`

**操作功能**:
- 查看密钥详情 (弹窗/抽屉)
- 启用/禁用密钥
- 导出公钥

#### 3.2.2 密钥生成

**路径**: `/keys/generate`

**布局**:
```
┌─────────────────────────────────────────────────────────────┐
│  [表单区域]                                                  │
│                                                              │
│  密钥类型: ○ SM2  ○ RSA (预留扩展)                          │
│                                                              │
│  密钥用途: ☑ 签名  ☑ 加密  ☐ 其他                           │
│                                                              │
│  有效期限: [日期选择器] 至 [日期选择器]                       │
│                                                              │
│  备注: [________________]                                    │
│                                                              │
│  [取消]                          [生成密钥]                  │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

**API映射**: `POST /mapi/keys/generate`

**交互逻辑**:
- 生成过程显示进度
- 完成后展示密钥ID与公钥
- 支持下载公钥文件

#### 3.2.3 密钥导入

**路径**: `/keys/import`

**布局**:
```
┌─────────────────────────────────────────────────────────────┐
│  [导入区域]                                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                                                      │   │
│  │     拖拽文件到此处，或 [点击上传]                     │   │
│  │     支持 .pem, .der 格式                             │   │
│  │                                                      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                              │
│  密钥类型: [自动识别 / 手动选择]                             │
│                                                              │
│  [取消]                              [导入密钥]             │
└─────────────────────────────────────────────────────────────┘
```

**API映射**: `POST /mapi/keys/import`

---

### 3.3 签名服务

#### 3.3.1 签名请求

**路径**: `/sign/request`

**布局**:
```
┌─────────────────────────────────────────────────────────────┐
│  [签名表单]                                                  │
│                                                              │
│  选择密钥: [下拉选择密钥列表]                                │
│                                                              │
│  签名算法: [SM2withSM3 ▼]                                   │
│                                                              │
│  待签名数据:                                                 │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                                                      │   │
│  │  [文本输入 或 文件上传]                              │   │
│  │                                                      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                              │
│  数据格式: ○ Hex  ○ Base64  ○ 原始文本                     │
│                                                              │
│  [取消]                              [执行签名]             │
└─────────────────────────────────────────────────────────────┘
```

**API映射**: `POST /api/sign`

**结果展示**:
```
┌─────────────────────────────────────────────────────────────┐
│  [签名结果]                                                  │
│                                                              │
│  签名值 (Base64):                                            │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ MEUCIQC2... (可复制)                                │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                              │
│  签名值 (Hex):                                               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 30440220... (可复制)                                │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                              │
│  [复制签名]  [验证签名]  [下载结果]                         │
└─────────────────────────────────────────────────────────────┘
```

#### 3.3.2 签名记录

**路径**: `/sign/records`

**布局**: 类似密钥列表的表格布局

**API映射**: `GET /mapi/sign/records`

**表格字段**: 记录ID、密钥ID、签名算法、请求时间、状态、操作

---

### 3.4 用户管理

#### 3.4.1 用户列表

**路径**: `/users/list`

**布局**:
```
┌─────────────────────────────────────────────────────────────┐
│  [搜索栏]                                                    │
│  用户名: [________] 角色: [下拉选择] 状态: [下拉选择]       │
│  [搜索] [重置]                                [新增用户]     │
├─────────────────────────────────────────────────────────────┤
│  [数据表格]                                                  │
│  ┌──────┬────────┬──────┬────────┬────────┬────────┬──────┐ │
│  │ 头像 │ 用户名  │ 角色  │ 邮箱    │ 创建时间│ 状态   │ 操作 │ │
│  ├──────┼────────┼──────┼────────┼────────┼────────┼──────┤ │
│  │ 🧑  │ admin  │ 超管 │ a@x.com│ 2024-01│ 启用   │ 编辑 │ │
│  │ 👤  │ user1  │ 运维 │ u@x.com│ 2024-02│ 启用   │ 编辑 │ │
│  └──────┴────────┴──────┴────────┴────────┴────────┴──────┘ │
└─────────────────────────────────────────────────────────────┘
```

**API映射**: 
- 列表: `GET /mapi/users`
- 新增: `POST /mapi/users`
- 编辑: `PUT /mapi/users/:id`
- 删除: `DELETE /mapi/users/:id`

#### 3.4.2 角色管理

**路径**: `/users/roles`

**预设角色**:

| 角色名称 | 角色标识 | 权限范围 |
|---------|---------|---------|
| 超级管理员 | super_admin | 全部权限 |
| 运维管理员 | ops_admin | 签名服务、日志查看 |
| 安全管理员 | sec_admin | 密钥管理 |
| 普通用户 | user | 基础签名权限 |

#### 3.4.3 权限配置

**路径**: `/users/permissions`

**布局**: 树形权限配置

---

### 3.5 系统设置

#### 3.5.1 基础配置

**路径**: `/settings/basic`

**配置项**:
- 服务名称
- 服务描述
- 会话超时时间
- 登录失败锁定策略

#### 3.5.2 安全策略

**路径**: `/settings/security`

**配置项**:
- 密码复杂度要求
- 双因素认证设置
- IP 白名单
- API 访问频率限制

#### 3.5.3 服务监控

**路径**: `/settings/monitor`

**展示内容**:
- 服务运行状态
- CPU/内存使用率
- 数据库连接池状态
- 最近告警信息

---

### 3.6 操作日志

#### 3.6.1 操作记录

**路径**: `/logs/operations`

**布局**:
```
┌─────────────────────────────────────────────────────────────┐
│  [筛选条件]                                                  │
│  时间范围: [开始日期] 至 [结束日期]                          │
│  操作类型: [下拉多选]  操作用户: [下拉选择]                  │
│  [查询] [重置] [导出]                                        │
├─────────────────────────────────────────────────────────────┤
│  [数据表格]                                                  │
│  ┌────────────┬────────┬──────────┬────────┬────────┬──────┐ │
│  │ 操作时间    │ 用户   │ 操作类型  │ 详情   │ IP地址 │ 状态 │ │
│  ├────────────┼────────┼──────────┼────────┼────────┼──────┤ │
│  │ 2024-01-01 │ admin │ 密钥生成  │ key_001│ 192... │ 成功 │ │
│  │ 2024-01-02 │ user1 │ 签名请求  │ sign...│ 192... │ 成功 │ │
│  └────────────┴────────┴──────────┴────────┴────────┴──────┘ │
└─────────────────────────────────────────────────────────────┘
```

**API映射**: `GET /mapi/logs/operations`

#### 3.6.2 登录日志

**路径**: `/logs/login`

**表格字段**: 时间、用户、登录方式、IP地址、设备信息、状态

---

## 4. 通用组件

### 4.1 页面头部组件

```typescript
interface PageHeaderProps {
  title: string;
  subtitle?: string;
  breadcrumbs?: BreadcrumbItem[];
  actions?: React.ReactNode;
}
```

### 4.2 搜索表单组件

```typescript
interface SearchFormProps {
  fields: SearchField[];
  onSearch: (values: Record<string, any>) => void;
  onReset: () => void;
}
```

### 4.3 数据表格组件

```typescript
interface DataTableProps<T> {
  columns: ColumnDef<T>[];
  dataSource: T[];
  loading?: boolean;
  pagination?: PaginationConfig;
  rowSelection?: RowSelectionConfig;
}
```

### 4.4 详情抽屉组件

```typescript
interface DetailDrawerProps {
  visible: boolean;
  title: string;
  data: Record<string, any>;
  fields: DetailField[];
  onClose: () => void;
}
```

---

## 5. 路由配置

```typescript
const routes: RouteConfig[] = [
  {
    path: '/dashboard',
    component: Dashboard,
    meta: { title: '仪表盘', icon: 'DashboardOutlined' }
  },
  {
    path: '/keys',
    meta: { title: '密钥管理', icon: 'KeyOutlined' },
    children: [
      { path: 'list', component: KeyList },
      { path: 'generate', component: KeyGenerate },
      { path: 'import', component: KeyImport }
    ]
  },
  {
    path: '/sign',
    meta: { title: '签名服务', icon: 'EditOutlined' },
    children: [
      { path: 'request', component: SignRequest },
      { path: 'records', component: SignRecords }
    ]
  },
  {
    path: '/users',
    meta: { title: '用户管理', icon: 'UserOutlined', permission: 'super_admin' },
    children: [
      { path: 'list', component: UserList },
      { path: 'roles', component: RoleManage },
      { path: 'permissions', component: PermissionConfig }
    ]
  },
  {
    path: '/settings',
    meta: { title: '系统设置', icon: 'SettingOutlined', permission: 'super_admin' },
    children: [
      { path: 'basic', component: BasicSettings },
      { path: 'security', component: SecuritySettings },
      { path: 'monitor', component: ServiceMonitor }
    ]
  },
  {
    path: '/logs',
    meta: { title: '操作日志', icon: 'FileTextOutlined' },
    children: [
      { path: 'operations', component: OperationLogs },
      { path: 'login', component: LoginLogs }
    ]
  }
];
```

---

## 6. 权限控制

### 6.1 权限矩阵

| 功能模块 | 超级管理员 | 运维管理员 | 安全管理员 | 普通用户 |
|---------|----------|----------|----------|---------|
| 仪表盘 | ✓ | ✓ | ✓ | ✓ |
| 密钥管理 | ✓ | ✗ | ✓ | ✗ |
| 签名请求 | ✓ | ✓ | ✓ | ✓ |
| 签名记录 | ✓ | ✓ | ✗ | ✗ |
| 用户管理 | ✓ | ✗ | ✗ | ✗ |
| 系统设置 | ✓ | ✗ | ✗ | ✗ |
| 操作日志 | ✓ | ✓ | ✗ | ✗ |

### 6.2 路由守卫

```typescript
// 路由权限守卫
router.beforeEach((to, from, next) => {
  const userStore = useUserStore();
  const requiredPermission = to.meta?.permission;
  
  if (requiredPermission && !hasPermission(userStore.role, requiredPermission)) {
    next('/403');
  } else {
    next();
  }
});
```

---

## 7. API 接口映射

| 前端页面 | HTTP方法 | API路径 | 说明 |
|---------|---------|---------|------|
| 密钥列表 | GET | /mapi/keys | 获取密钥列表 |
| 密钥生成 | POST | /mapi/keys/generate | 生成新密钥 |
| 密钥导入 | POST | /mapi/keys/import | 导入密钥 |
| 密钥详情 | GET | /mapi/keys/:id | 获取密钥详情 |
| 签名请求 | POST | /api/sign | 执行签名 |
| 签名记录 | GET | /mapi/sign/records | 签名记录列表 |
| 用户列表 | GET | /mapi/users | 用户列表 |
| 用户创建 | POST | /mapi/users | 创建用户 |
| 用户更新 | PUT | /mapi/users/:id | 更新用户 |
| 用户删除 | DELETE | /mapi/users/:id | 删除用户 |
| 操作日志 | GET | /mapi/logs/operations | 操作日志 |
| 登录日志 | GET | /mapi/logs/login | 登录日志 |

---

## 8. 状态管理

### 8.1 全局状态

```typescript
interface GlobalState {
  user: {
    info: UserInfo | null;
    permissions: string[];
    token: string | null;
  };
  app: {
    collapsed: boolean;        // 侧边栏折叠状态
    theme: 'light' | 'dark';   // 主题
    locale: string;            // 语言
  };
  keys: {
    list: KeyInfo[];
    current: KeyInfo | null;
    loading: boolean;
  };
  logs: {
    operations: LogRecord[];
    login: LogRecord[];
  };
}
```

---

## 9. 响应式设计

### 9.1 断点设置

| 断点名称 | 屏幕宽度 | 布局调整 |
|---------|---------|---------|
| xs | < 576px | 侧边栏隐藏，移动端菜单 |
| sm | ≥ 576px | 侧边栏可收起 |
| md | ≥ 768px | 侧边栏固定显示 |
| lg | ≥ 992px | 标准桌面布局 |
| xl | ≥ 1200px | 宽屏布局 |
| xxl | ≥ 1600px | 超宽屏布局 |

### 9.2 移动端适配

- 侧边栏改为抽屉式弹出
- 表格横向滚动或卡片展示
- 表单单列布局

---

## 10. 国际化支持

### 10.1 语言包结构

```json
{
  "zh-CN": {
    "menu": {
      "dashboard": "仪表盘",
      "keys": "密钥管理",
      "sign": "签名服务"
    },
    "action": {
      "search": "搜索",
      "reset": "重置",
      "submit": "提交"
    }
  },
  "en-US": {
    "menu": {
      "dashboard": "Dashboard",
      "keys": "Key Management",
      "sign": "Sign Service"
    },
    "action": {
      "search": "Search",
      "reset": "Reset",
      "submit": "Submit"
    }
  }
}
```

---

## 11. 错误处理

### 11.1 错误码映射

| 错误码 | 提示信息 | 处理方式 |
|-------|---------|---------|
| 401 | 登录已过期 | 跳转登录页 |
| 403 | 无权限访问 | 提示并跳转首页 |
| 404 | 资源不存在 | 显示404页面 |
| 500 | 服务器错误 | 提示并记录日志 |

### 11.2 全局错误拦截

```typescript
// Axios 拦截器
axios.interceptors.response.use(
  response => response,
  error => {
    const { status, data } = error.response || {};
    
    switch (status) {
      case 401:
        message.error('登录已过期，请重新登录');
        router.push('/login');
        break;
      case 403:
        message.error('无权限访问');
        router.push('/403');
        break;
      case 500:
        message.error(data?.message || '服务器错误');
        break;
      default:
        message.error('网络错误，请稍后重试');
    }
    
    return Promise.reject(error);
  }
);
```

---

## 12. 技术栈推荐

| 类别 | 技术选型 | 备选方案 |
|-----|---------|---------|
| 框架 | React 18+ | Vue 3+ |
| UI库 | Ant Design 5.x | TDesign |
| 状态管理 | Zustand | Pinia / Redux Toolkit |
| 路由 | React Router 6 | Vue Router 4 |
| HTTP | Axios | Fetch API |
| 图表 | ECharts | Chart.js |
| 构建 | Vite | Webpack |
| 语言 | TypeScript | - |

---

## 13. 项目目录结构

```
admin-frontend/
├── public/
│   └── favicon.ico
├── src/
│   ├── api/                 # API 接口
│   │   ├── keys.ts
│   │   ├── sign.ts
│   │   ├── users.ts
│   │   └── logs.ts
│   ├── components/          # 通用组件
│   │   ├── PageHeader/
│   │   ├── SearchForm/
│   │   ├── DataTable/
│   │   └── DetailDrawer/
│   ├── layouts/             # 布局组件
│   │   └── AdminLayout/
│   ├── pages/               # 页面组件
│   │   ├── dashboard/
│   │   ├── keys/
│   │   ├── sign/
│   │   ├── users/
│   │   ├── settings/
│   │   └── logs/
│   ├── router/              # 路由配置
│   │   └── index.ts
│   ├── stores/              # 状态管理
│   │   ├── user.ts
│   │   ├── keys.ts
│   │   └── app.ts
│   ├── styles/              # 样式文件
│   ├── utils/               # 工具函数
│   ├── hooks/               # 自定义 Hooks
│   ├── types/               # 类型定义
│   ├── i18n/                # 国际化
│   ├── App.tsx
│   └── main.tsx
├── .env.development
├── .env.production
├── package.json
├── tsconfig.json
└── vite.config.ts
```

---

## 14. 开发规范

### 14.1 命名规范

- 组件文件: PascalCase (如 `UserList.tsx`)
- 工具函数: camelCase (如 `formatDate.ts`)
- 样式文件: kebab-case (如 `user-list.module.scss`)
- 常量: UPPER_SNAKE_CASE (如 `API_BASE_URL`)

### 14.2 Git 提交规范

```
feat: 新功能
fix: 修复问题
docs: 文档更新
style: 代码格式调整
refactor: 代码重构
test: 测试相关
chore: 构建/工具相关
```

---

## 附录 A: 页面交互流程图

### A.1 密钥生成流程

```
用户点击"生成密钥" 
    → 填写表单 
    → 提交请求 
    → 显示加载状态 
    → 服务端生成密钥对 
    → 返回结果 
    → 展示密钥ID与公钥 
    → 用户可下载公钥
```

### A.2 签名请求流程

```
用户选择密钥 
    → 输入/上传待签名数据 
    → 选择数据格式 
    → 提交签名请求 
    → 服务端协同签名 
    → 返回签名结果 
    → 展示签名值 (Base64/Hex) 
    → 用户可复制/下载
```

---

## 附录 B: 参考资料

- [Ant Design 官方文档](https://ant.design/)
- [React Router 文档](https://reactrouter.com/)
- [ECharts 文档](https://echarts.apache.org/)
- [Zustand 文档](https://github.com/pmndrs/zustand)

---

**文档版本**: v1.0  
**创建日期**: 2024-01-15  
**最后更新**: 2024-01-15  
**维护者**: 开发团队
